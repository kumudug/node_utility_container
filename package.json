{
  "name": "app",
  "version": "1.0.0",
  "description": "* Build    - `docker build . -t node-util --build-arg USER_ID=$(id -u) --build-arg GROUP_ID=$(id -g)` * Run     - When running we are gonna give he command to run    - Add a bind mount    - The idea is to npm init a project using the docker container inside of the host machine instead of installing node in the host machine    - `docker run -it -v $(pwd):/app --rm node-util npm init`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kumudug/node_utility_container.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kumudug/node_utility_container/issues"
  },
  "homepage": "https://github.com/kumudug/node_utility_container#readme"
}
